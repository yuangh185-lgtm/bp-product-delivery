-- Replace names and sizes with confirmed domain definitions.
CREATE TABLE BP_ENTITY (
    ID                NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    BUSINESS_CODE     VARCHAR2(64 CHAR) NOT NULL,
    BUSINESS_STATUS   VARCHAR2(32 CHAR) NOT NULL,
    VERSION_NO        NUMBER(10) DEFAULT 0 NOT NULL,
    CREATED_BY        VARCHAR2(64 CHAR) NOT NULL,
    CREATED_AT        TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL,
    UPDATED_BY        VARCHAR2(64 CHAR) NOT NULL,
    UPDATED_AT        TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL,
    CONSTRAINT PK_BP_ENTITY PRIMARY KEY (ID),
    CONSTRAINT UK_BP_ENTITY_CODE UNIQUE (BUSINESS_CODE)
);

COMMENT ON TABLE BP_ENTITY IS 'Replace with confirmed entity description';
COMMENT ON COLUMN BP_ENTITY.BUSINESS_CODE IS 'Replace with confirmed business identifier';

